import { OMILayout } from "@/components/orbit-marketing-intelligence/omi-layout"

export default function OrbitMIMainLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <OMILayout>{children}</OMILayout>
}
